package com.yahorshymanchyk.mylamp.secrets

/**
 * Template. Copy this file to `Secrets.kt` (without `.template`) in the same folder
 * and fill in real values. `Secrets.kt` is in .gitignore and never pushed.
 */
object Secrets {

    // WiFi screen (direct MQTT) — Mosquitto broker on the Pi
    const val MQTT_HOST = "192.168.1.36"
    const val MQTT_PORT = 1883
    const val MQTT_USERNAME = ""
    const val MQTT_PASSWORD = ""

    // REST screen (Home Assistant)
    const val HA_BASE_URL = "http://192.168.1.36:8123"
    const val HA_LONG_LIVED_TOKEN = ""
    const val HA_LAMP_ENTITY_ID = "light.lamp_cabinet"

    // BLE screen (directly to the Pi)
    const val BLE_SERVICE_UUID = ""
}
